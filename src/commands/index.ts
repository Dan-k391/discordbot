import * as ping from './ping';
import * as join from './join';
import * as voicestats from './voicestats'
import * as voiceleaderboard from './voiceleaderboard'

export const commands = {
    ping,
    join,
    voicestats,
    voiceleaderboard
};
